# How to run 

## Docker

```bash
#App-client local docker
cd client/

docker-compose -f docker-compose.yml up -d
```

```bash
#App-server local docker
cd server/

docker-compose -f docker-compose.yml up -d
```